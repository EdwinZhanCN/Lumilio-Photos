# Introduction

Lumilio Photos keeps media in repositories on storage you control. Desktop
provides a machine-local default Storage Location; additional external
locations can be authorized later without moving the database or credentials.

Before placing media on another disk, read
[Storage Locations and Repositories](./repositories.md). It explains why **Add
location**, **Create repository**, and **Open existing repository** are separate
operations.

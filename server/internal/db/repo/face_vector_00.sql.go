// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: face_vector_00.sql

package repo

import (
	"context"
)

const getUnclusteredFacesInScope = `-- name: GetUnclusteredFacesInScope :many
SELECT fi.id, fi.asset_id, fi.face_id, fi.bounding_box, fi.confidence, fi.age_group, fi.gender, fi.ethnicity, fi.expression, fi.face_size, fi.face_image_path, fi.embedding, fi.embedding_model, fi.is_primary, fi.quality_score, fi.blur_score, fi.pose_angles, fi.created_at
FROM face_items fi
JOIN assets a ON a.asset_id = fi.asset_id
LEFT JOIN face_cluster_members fcm ON fi.id = fcm.face_id
WHERE fcm.face_id IS NULL
  AND COALESCE(a.is_deleted, false) = false
  AND (?1 IS NULL OR a.repository_id = ?1)
  AND COALESCE(a.owner_id, -1) = COALESCE(?2, -1)
  AND COALESCE(fi.embedding_model, '') = COALESCE(?3, '')
  AND fi.embedding IS NOT NULL
  AND fi.confidence >= ?4
  AND COALESCE(fi.face_size, 0) >= ?5
ORDER BY fi.confidence DESC, COALESCE(fi.face_size, 0) DESC, fi.id ASC
`

type GetUnclusteredFacesInScopeParams struct {
	RepositoryID   interface{} `db:"repository_id" json:"repository_id"`
	OwnerID        *int32      `db:"owner_id" json:"owner_id"`
	EmbeddingModel *string     `db:"embedding_model" json:"embedding_model"`
	MinConfidence  float64     `db:"min_confidence" json:"min_confidence"`
	MinFaceSize    *int64      `db:"min_face_size" json:"min_face_size"`
}

func (q *Queries) GetUnclusteredFacesInScope(ctx context.Context, arg GetUnclusteredFacesInScopeParams) ([]FaceItem, error) {
	rows, err := q.db.QueryContext(ctx, getUnclusteredFacesInScope,
		arg.RepositoryID,
		arg.OwnerID,
		arg.EmbeddingModel,
		arg.MinConfidence,
		arg.MinFaceSize,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FaceItem
	for rows.Next() {
		var i FaceItem
		if err := rows.Scan(
			&i.ID,
			&i.AssetID,
			&i.FaceID,
			&i.BoundingBox,
			&i.Confidence,
			&i.AgeGroup,
			&i.Gender,
			&i.Ethnicity,
			&i.Expression,
			&i.FaceSize,
			&i.FaceImagePath,
			&i.Embedding,
			&i.EmbeddingModel,
			&i.IsPrimary,
			&i.QualityScore,
			&i.BlurScore,
			&i.PoseAngles,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

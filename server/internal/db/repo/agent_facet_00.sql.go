// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: agent_facet_00.sql

package repo

import (
	"context"
	"strings"

	"github.com/google/uuid"
)

const agentFacetOverview = `-- name: AgentFacetOverview :one
SELECT
    COUNT(*) AS total,
    MIN(COALESCE(a.taken_time, a.upload_time)) AS date_from,
    MAX(COALESCE(a.taken_time, a.upload_time)) AS date_to,
    COUNT(*) FILTER (WHERE a.liked = true) AS liked_count,
    COALESCE(MIN(a.capture_offset_minutes), 0) AS capture_offset_minutes
FROM assets a
WHERE a.asset_id IN (/*SLICE:asset_ids*/?)
  AND a.is_deleted = false
`

type AgentFacetOverviewRow struct {
	Total                int64       `db:"total" json:"total"`
	DateFrom             interface{} `db:"date_from" json:"date_from"`
	DateTo               interface{} `db:"date_to" json:"date_to"`
	LikedCount           int64       `db:"liked_count" json:"liked_count"`
	CaptureOffsetMinutes interface{} `db:"capture_offset_minutes" json:"capture_offset_minutes"`
}

func (q *Queries) AgentFacetOverview(ctx context.Context, assetIds []uuid.UUID) (AgentFacetOverviewRow, error) {
	query := agentFacetOverview
	var queryParams []interface{}
	if len(assetIds) > 0 {
		for _, v := range assetIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:asset_ids*/?", strings.Repeat(",?", len(assetIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:asset_ids*/?", "NULL", 1)
	}
	row := q.db.QueryRowContext(ctx, query, queryParams...)
	var i AgentFacetOverviewRow
	err := row.Scan(
		&i.Total,
		&i.DateFrom,
		&i.DateTo,
		&i.LikedCount,
		&i.CaptureOffsetMinutes,
	)
	return i, err
}

/** Settings-owned compatibility exports for the shared persisted preference model. */
export {
  DEFAULT_PREFERENCES,
  useDebouncedPreference,
  usePreference,
  usePreferencesStore,
} from "@/lib/preferences/preferences";
export type { AssetPagePreferences, Preferences } from "@/lib/preferences/preferences";
